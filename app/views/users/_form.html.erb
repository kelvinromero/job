<%= form_with(model: user, local: true) do |form| %>
  <% if user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
        <% user.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :first_name, class: 'label' %>
    <div class="control">
      <%= form.text_field :first_name, class: "input is-small" %>
    </div>
  </div>

  <div class="field">
    <%= form.label :last_name, class: 'label' %>
    <div class="control">
      <%= form.text_field :last_name, class: "input is-small" %>
    </div>
  </div>

  <div class="actions">
    <button class="button is-light" type="submit"> Save </button>
  </div>
<% end %>
